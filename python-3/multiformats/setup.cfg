[metadata]
name = multiformats
version = 0.0.1

[options]
packages = find_namespace:

[options.extras_require]
testing =
	setuptools
	pytest
	pytest-cov

[tool:pytest]
testpaths = test

[coverage:run]
branch = True

[coverage:report]
show_missing = True
skip_covered = True
