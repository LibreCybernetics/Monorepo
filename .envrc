#!/usr/bin/env bash
# From: https://github.com/numtide/devshell/blob/3e0e60ab37cd0bf7ab59888f5c32499d851edb47/templates/toml/.envrc

if [[ $(type -t use_flake) != function ]]; then
  echo "ERROR: use_flake function missing."
  echo "Please update direnv to v2.30.0 or later."
  exit 1
fi
use flake
